# QUASAR v15 Fix Status — 2026-06-17 21:45

## Fix Summary

The broken `sed` patch (fix14) introduced two syntax/logic errors in `quasar_v15.py`:

| Location | Bug | Fix |
|---|---|---|
| Line 1165 (`_r_target` body) | `budget = kw.get(fidelity, budget)` — 12-space indent, no quotes | `budget = kw.get('fidelity', budget)` — 24-space indent, quoted |
| Line 1271 (`OuterTargetFactory.__call__`) | `budget = kwargs.get(fidelity, budget)` — no quotes | `budget = kwargs.get('fidelity', budget)` — 8-space indent, quoted |

**Root cause**: The `sed` command inserted the budget line at 4-space indent instead of matching the function body's indentation level. The `_r_target` function is defined at 20-space indent (nested inside a `try` block inside a loop), so its body requires 24-space indent.

## Fix Scripts Applied

1. `fix15_exact.py` — Added quotes around `'fidelity'` in both locations
2. `fix16_indent.py` — Corrected indentation of line 1165 from 12→24 spaces

**Result**: `python3 -c "import ast; ast.parse(open('quasar_v15.py').read())"` → SYNTAX_OK

## Job Status

| Job | Seed | Status |
|---|---|---|
| 181583 | 42 | KILLED (broken code) |
| 181584 | 123 | KILLED (broken code) |
| 181585 | 456 | KILLED (broken code) |
| 181589 | 42 dup | KILLED (broken code) |
| **181592** | **42** | **RUNNING (fixed)** |
| **181593** | **123** | **RUNNING (fixed)** |
| **181594** | **456** | **RUNNING (fixed)** |

## Expected Results

First result JSONs at `~/quasar_v15/results/` in ~2–3h from submission.
Baseline reference: GHZ 0.991, W 0.987, Cluster 0.977 (pre-fix10, default HP).
DEHB outer should now find better HP than default α=0.05.
